import{describe,expect,it}from'vitest';import{battleWinner,calculateDamage,canPlace,deserialize,eraReady,freshGame,happinessMultiplier,occupied,productionReady,roadConnected,serialize,techAvailable}from'./rules';
describe('pravidla města',()=>{const game=freshGame();it('eviduje obsazená pole',()=>expect(occupied(game.buildings).has('8,7')).toBe(true));it('odmítne překrytí a okraj',()=>{expect(canPlace(game.buildings,'hut',8,7)).toBe(false);expect(canPlace(game.buildings,'hut',19,19)).toBe(false);expect(canPlace(game.buildings,'hut',0,0)).toBe(true)});it('projde souvislou síť cest',()=>expect(roadConnected(game.buildings.find(b=>b.id==='work1')!,game.buildings)).toBe(true))});
describe('ekonomika a čas',()=>{it('počítá stupně spokojenosti',()=>{expect(happinessMultiplier(40)).toBe(.7);expect(happinessMultiplier(95)).toBe(1.1);expect(happinessMultiplier(130)).toBe(1.2)});it('dokončí produkci podle absolutního času',()=>expect(productionReady(100,101)).toBe(true))});
describe('postup a boj',()=>{it('ověří technologii a epochu',()=>{expect(techAvailable('tools',{researched:[],epoch:1})).toBe(true);expect(eraReady(['charter'])).toBe(true)});it('počítá kontrolovatelné poškození',()=>expect(calculateDamage(12,10,0)).toBe(7));it('určí vítěze',()=>expect(battleWinner([{side:'player',hp:5},{side:'enemy',hp:0}])).toBe('player'))});
it('serializuje a bezpečně načte hru',()=>{const g=freshGame();expect(deserialize(serialize(g))?.cityName).toBe(g.cityName);expect(deserialize('{bad')).toBeNull()});
